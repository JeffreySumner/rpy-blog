# Diabetes Risk Analysis

This post is a project overview and showcase.

## Setup

**No special setup is required.**

This document primarily consists of text and an embedded `iframe` pointing to a Shiny application hosted on `shinyapps.io`. It does not execute local code for analysis or modeling during the render process.
